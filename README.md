# Weather Desk

Clean, glassy weather UI built with React, Vite, and MUI.

## Setup

1. Copy `.env.example` to `.env`
2. Set `VITE_WEATHER_API_KEY` (OpenWeather API key)

## Scripts

- `npm run dev`
- `npm run build`
- `npm run lint`
